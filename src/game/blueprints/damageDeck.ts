import type { CardBlueprint } from '../types'

// Damage is temporarily disabled while the open-mission economy is being
// tuned. The deck is empty so no Damage cards can be drawn even if a Gate
// fails to clear cleanly. Restore the original cards from git when re-enabling.
export const damageDeck: CardBlueprint[] = []
